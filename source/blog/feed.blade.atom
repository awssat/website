---
site_path: blog
---
@extends('_layouts.blog.partial.rss')

@section('entries')
    @foreach ($posts_en as $entry)
        @php $entryUrl = $page->getPostUrl($entry); @endphp
        <entry>
            <id>{{ $entryUrl }}</id>
            <link type="text/html" rel="alternate" href="{{ $entryUrl }}" />
            <title>{{ $entry->title }}</title>
            <published>{{ date(DATE_ATOM, $entry->date) }}</published>
            <updated>{{ date(DATE_ATOM, $entry->date) }}</updated>
            <author>
                <name>{{ $entry->author }}</name>
            </author>
            <summary type="html">{{ $entry->getExcerpt() }}...</summary>
            <content type="html"><![CDATA[{!! $entry->getContent() !!}]]></content>
        </entry>
    @endforeach
@endsection
